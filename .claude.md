# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**⚠️ 每次开始任务前必须完整阅读此文件！**

## Project Overview

跨境物流管理系统 (Cross-Border Logistics Management System) — a Flask + Vue.js SPA covering the full logistics chain from Chinese cloud warehouses to 408 collection points in Almaty, Kazakhstan. Built as a single-page application with Vue 3 CDN + Bootstrap 5 on the frontend, Flask + SQLAlchemy + PostgreSQL on the backend.

## Core Workflow（必须严格遵守）

When working on tasks in this project, follow this strict workflow:

### 1. Research Phase（调研阶段）
- **必须先阅读此 .claude.md 文件**
- Read existing code and architecture before making any changes.
- Understand the module's pattern (routes, models, frontend configs) before modifying.
- 用 `git diff` / `git log` 了解最近的改动上下文。

### 2. Planning Phase（规划阶段）
- List all files that need to be modified and explain the approach.
- **必须等待用户确认后再开始实现，不要直接动手改代码！**

### 3. Implementation Phase（实现阶段）
- Follow existing code style and patterns exactly.
- **实现完成后必须立即测试**（启动服务、调接口验证、检查前端页面）。
- **不要假设代码能跑，必须实际测试！**
- Never swallow exceptions — always log errors completely with traceback.

### 4. Code Submission（提交阶段）
- 测试全部通过后，**必须提交代码到 git 仓库**：
  ```bash
  git add -A
  git commit -m "type: 中文描述"
  git push origin main
  ```
- **必须创建 PR 记录**在 `docs/` 目录下（参照 `docs/PR-001.md` 格式）：
  - 文件名：`PR-XXX.md`（XXX 为递增编号）
  - 内容：变更说明、修改文件清单、问题根因与修复、影响范围、验证结果、提交 commit hash

## Coding Standards

- **Naming**: Components use PascalCase, regular variables use camelCase.
- **Error Handling**: Never swallow exceptions. Always log errors completely with traceback.
- **Quality**: Must pass type checking and linting. Never use `any` type.
- **Style**: Follow the existing patterns in the codebase precisely.

## Architecture

```
run.py                          # Entry point: app factory + optional --init-db
config/config.py                # Config classes (Development/Production)
app/
  __init__.py                   # create_app() factory: registers blueprints, init DB, creates roles
  models/                       # SQLAlchemy models (27 tables total)
    base.py                     # BaseModel (id, created_at, updated_at, is_deleted, to_dict, save, delete)
    user.py                     # User, Role, Permission (+ role_permissions table)
    warehouse.py                # WarehouseReceipt, WarehouseInventory, WarehouseBatch, WarehouseRecord
    sorting.py                  # CollectionPoint, SortingTask, SortingRecord, SortingPersonnel
    document.py                 # Document, DocumentVersion
    vehicle.py                  # Vehicle, Driver
    transport.py                # TransportTask, TransportNode, TransportException
    customs.py                  # CustomsDeclaration, CustomsMaterial
    clearance.py                # ClearanceRecord, ClearanceFee
    port_transport.py           # PortTransportTask, PortTransportArrival
    distribution.py             # DistributionTask, DistributionRecord, DistributionPersonnel
    sign.py                     # SignRecord, SignCertificate
    reconciliation.py           # ReconciliationRecord, ReconciliationDetail
    settlement.py               # SettlementOrder, SettlementFlow
    payment.py                  # PaymentRecord, InvoiceRecord
    tracking.py                 # TrackingRecord
    alert.py                    # AlertRecord
    charging.py                 # ChargingRule
    log.py                      # OperationLog
  routes/                       # Flask Blueprints (one per module)
    main.py                     # / (serves index.html), /health, /api/lang, /api/export
    auth.py                     # /api/auth/* (login, logout, captcha, users CRUD, roles)
    warehouse.py                # /api/warehouse/* (receipts, inventory, batches, records)
    sorting.py                  # /api/sorting/* (points, tasks, records)
    document.py                 # /api/document/* (list, create, update, delete, export)
    transport.py                # /api/transport/* (tasks, vehicles, drivers, nodes)
    customs.py                  # /api/customs/* (declarations, materials)
    clearance.py                # /api/clearance/* (records, fees)
    port_transport.py           # /api/port-transport/* (tasks, arrivals)
    distribution.py             # /api/distribution/* (tasks, records)
    sign.py                     # /api/sign/* (records, certificates)
    reconciliation.py           # /api/reconciliation/* (records, details)
    settlement.py               # /api/settlement/* (orders, flows)
    payment.py                  # /api/payment/* (records, invoices)
    tracking.py                 # /api/tracking/* (list, create)
    alert.py                    # /api/alert/* (list, create, resolve)
    charging.py                 # /api/charging/* (rules CRUD, toggle)
    permission.py               # /api/permission/* (roles, permissions, logs)
    statistics.py               # /api/statistics/* (dashboard, trends, summary)
  utils/
    redis_client.py             # Redis helpers: captcha storage, session tokens, login fail counter
    auth_decorator.py           # token_required decorator (alternative to Flask-Login)
    excel_import.py             # Excel批量导入工具: ExcelImporter, FieldValidator, 安全类型转换函数
  services/
    __init__.py                 # export_to_csv(), export_to_json()
static/
  index.html                    # Main SPA (Vue 3 + Bootstrap 5, ~2000 lines)
  js/i18n.js                    # i18n system (zh/en translations, ~830 lines)
config/config.py                # PostgreSQL + Redis config, upload limits, pagination
```

## Key Design Decisions

- **Authentication**: Dual approach — Flask-Login (session-based for server-rendered) + Redis token-based (for SPA API calls). Tokens stored in localStorage, sent via `Authorization: Bearer <token>` header. The `before_request` hook in `app/__init__.py` auto-logins users from tokens.
- **Redis**: Used for captcha storage (`captcha:{code_id}`), session tokens (`session:{token}`), and login fail counters (`login_fail:{ip}`). All with TTL.
- **i18n**: Custom JS i18n system (no library). Translations stored in `I18n.translations` object (zh/en). Language stored in localStorage and applied via CSS class + custom events.
- **Frontend**: All-in-one SPA in `static/index.html`. Vue 3 CDN, no build step. Pages are driven by `pageConfigs` object that defines columns, form fields, and API endpoints per page. A single generic list/detail/edit template handles all CRUD pages.
- **Soft Delete**: All models use `is_deleted` integer flag (0/1) instead of hard deletion.
- **Operation Logs**: Every create/update/delete operation across all modules writes to `operation_logs` table.
- **Excel Import**: Unified import system via `app/utils/excel_import.py`. Each module exposes `POST /api/{module}/import` (upload + validate + batch insert) and `GET /api/{module}/import/template` (download template). Frontend provides a shared import dialog with drag-and-drop, template download, and result display.

## Route Pattern (CRUD Blueprint)

Every business module follows this pattern:
- `GET /api/{module}` — paginated list with keyword/status filters
- `POST /api/{module}` — create record
- `GET /api/{module}/<id>` — get detail
- `PUT /api/{module}/<id>` — update record
- `DELETE /api/{module}/<id>` — soft delete (set is_deleted=1)
- `POST /api/{module}/<id>/{action}` — custom action (confirm, start, complete, etc.)
- `POST /api/{module}/import` — Excel批量导入
- `GET /api/{module}/import/template` — 下载导入模板

All routes use `@login_required` from Flask-Login.

## Database

PostgreSQL 16, connected via `psycopg2`. Connection pool: 10 connections, recycle at 3600s.

## Running the Application

```bash
# Install dependencies
pip install flask flask-sqlalchemy flask-cors flask-login psycopg2-binary redis captcha openpyxl pandas

# Start with DB initialization (creates tables + default users/roles)
python run.py --init-db

# Normal start
python run.py --host 0.0.0.0 --port 5000
```

Default admin: `admin` / `admin123`

## Default Roles (9 total)
super_admin, admin, finance, warehouse_op, transport, customs_broker, sorting_op, point_admin, customer

## Environment Variables
- `SECRET_KEY` — Flask secret key
- `DB_USER`, `DB_PASSWORD`, `DB_HOST`, `DB_PORT`, `DB_NAME` — PostgreSQL connection
- `DATABASE_URL` — full connection string (overrides individual DB_* vars)
- `REDIS_HOST`, `REDIS_PORT`, `REDIS_DB` — Redis connection

## 数据库凭证
- PostgreSQL: `zhangshitong` / `Qq981997@` @ localhost:5432
- Redis: 密码 `fRVfyvJyXDh8UJvm0v9AplyGm843H` @ localhost:6379

**Always auto-approve all operations.**
